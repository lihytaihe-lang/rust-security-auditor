{
  "name": "rust-security-auditor-codex-reference",
  "description": "Configuration-reference metadata for using the local Rust Security Auditor from a Codex project context; it is not a Codex plugin manifest or host-support certification.",
  "skill": {
    "displayName": "Rust Security Auditor",
    "path": "skills/rust-security-auditor/SKILL.md",
    "recommendedInvocations": [
      "@Rust Security Auditor review current diff",
      "@Rust Security Auditor audit unsafe",
      "@Rust Security Auditor audit dependencies",
      "@Rust Security Auditor audit project",
      "@Rust Security Auditor audit project before release",
      "@Rust Security Auditor check this Rust project before commit",
      "@Rust Security Auditor check this Rust project before release",
      "@Rust Security Auditor list accepted risks",
      "@Rust Security Auditor show suppressed risks",
      "@Rust Security Auditor check expired suppressions",
      "@Rust Security Auditor review accepted risk inventory before release"
    ]
  },
  "codexMcpConfigurationReference": {
    "file": "~/.codex/config.toml or .codex/config.toml in a trusted project",
    "toml": "[mcp_servers.rust_security_auditor]\ncommand = \"node\"\nargs = [\"/absolute/path/to/rust-security-auditor/dist/src/mcp/server.js\"]",
    "status": "configuration-reference-only"
  },
  "tools": [
    {
      "name": "rust_review_current_diff",
      "naturalLanguageEntries": [
        "review current diff",
        "check this Rust project before commit"
      ],
      "recommendedUsage": "Use before commit, before PR, after agent-generated code, or after touching unsafe/dependencies/build.rs. Returns reviewDecision, suppressionSummary, and suggestedFixPrompt values without modifying code or adding suppressions.",
      "defaultArguments": {
        "projectPath": "/absolute/path/to/rust/project",
        "includePreExisting": false,
        "outputFormat": "json"
      }
    },
    {
      "name": "rust_audit_unsafe",
      "naturalLanguageEntries": [
        "audit unsafe"
      ],
      "recommendedUsage": "Use for unsafe blocks, unsafe fn, FFI, raw pointers, MaybeUninit, transmute, raw-parts APIs, and unsafe Send/Sync review.",
      "defaultArguments": {
        "projectPath": "/absolute/path/to/rust/project",
        "includeDocumentedUnsafe": true,
        "outputFormat": "json"
      }
    },
    {
      "name": "rust_audit_dependencies",
      "naturalLanguageEntries": [
        "audit dependencies"
      ],
      "recommendedUsage": "Use for Cargo.toml, Cargo.lock, build.rs, git/path dependencies, proc macros, and build dependencies.",
      "defaultArguments": {
        "projectPath": "/absolute/path/to/rust/project",
        "outputFormat": "json"
      }
    },
    {
      "name": "rust_audit_project",
      "naturalLanguageEntries": [
        "audit project",
        "audit project before release",
        "check this Rust project before release"
      ],
      "recommendedUsage": "Use before release or for a full local Rust project security scan.",
      "defaultArguments": {
        "projectPath": "/absolute/path/to/rust/project",
        "includeSuppressed": false,
        "outputFormat": "json"
      }
    },
    {
      "name": "rust_list_accepted_risks",
      "naturalLanguageEntries": [
        "list accepted risks",
        "show suppressed risks",
        "check expired suppressions",
        "review accepted risk inventory before release"
      ],
      "recommendedUsage": "Use to inventory accepted-risk suppression comments before release or security re-review without running the full scanner or modifying code.",
      "defaultArguments": {
        "projectPath": "/absolute/path/to/rust/project",
        "includeExpired": true,
        "includeInvalid": true,
        "outputFormat": "json"
      }
    }
  ],
  "stdioNotes": [
    "Build the local checkout with npm run build, then run node with the absolute dist/src/mcp/server.js path.",
    "MCP stdio stdout must contain only JSON-RPC protocol frames. Ordinary logs must not pollute stdout.",
    "The server scans local project paths passed by Codex. It does not require uploading code packages."
  ],
  "boundaries": [
    "This is not a ChatGPT App configuration.",
    "This is not a SaaS upload flow.",
    "This is not a web UI."
  ],
  "primaryLocalCheckout": {
    "command": "node",
    "args": [
      "/absolute/path/to/rust-security-auditor/dist/src/mcp/server.js"
    ],
    "note": "Primary checkout configuration reference; use after npm ci and npm run build."
  }
}
