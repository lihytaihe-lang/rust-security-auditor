[package]
name = "dependency-risk"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[lib]
proc-macro = true

[dependencies]
gitdep = { git = "https://github.com/example/gitdep", branch = "main" }
local_dep = { path = "local_dep" }

[build-dependencies]
cc = "1"
